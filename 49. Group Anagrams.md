# 49. Group Anagrams

---

```python
class Solution:
    def groupAnagrams(self, strs: List[str]) -> List[List[str]]:
        res = defaultdict(list)        # Mapping the char count to list of the Anagram
        for s in strs:
            count = [0] * 26
            for each_char in s:
                count[ord(each_char) - ord("a")] += 1

            res[tuple(count)].append(s)

        return list(res.values()) 
```

---

- 核心思想
用 26 个字母的出现次数作为 key
对每个字符串统计频次 count[0..25]，将 tuple(count) 作为字典键进行分组。

- 注意事项
key 必须可哈希：tuple(count) 可哈希、不可变；list 不能做 key。
字典里存 list 要先初始化：用 defaultdict(list) 省去手动建空列表。

返回类型：题目需要 List[List[str]]，因此用 list(res.values()) 把所有组取出来。

- 复杂度
设有 N 个字符串、每个最大长度 K：时间 O(N·K)，空间 O(N·K)。

- 常见坑
忘记把 count 转成 tuple 当 key。

用普通 dict 直接 res[key].append() 导致 KeyError。

返回了 res（字典）而不是 list(res.values())。
