# 128. Longest Consecutive Sequence

--

```python
class Solution:
    def longestConsecutive(self, nums: List[int]) -> int:
        longest = 0
        unique_nums = set(nums)
        for num in unique_nums:
            if (num - 1) not in unique_nums:
                new_num = num
                while new_num in unique_nums:
                    new_num += 1
                longest = max(longest, new_num - num)
            
        return longest
```python

---

刚看到这个提的时候脑子里有一种解法sort，再遍历一遍就妥了，耗时 O(n log n) -> 排序 O(n log n)， 再遍历是 O(n)。
但是题目要求是O(n), 经过学习了解到要用hash.
这里划我自己的重点！ set 和list的查找方法竟然不一样！！！list cost O(n), but set cost only O(1),具体原因我还没搞明白
